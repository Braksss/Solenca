{
  "name": "solenca",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm -r run dev",
    "build": "pnpm -r run build",
    "lint": "pnpm -r run lint"
  },
  "dependencies": {
    "matter-js": "^0.20.0",
    "react-icons": "^5.5.0"
  },
  "devDependencies": {
    "@types/matter-js": "^0.19.8"
  }
}
